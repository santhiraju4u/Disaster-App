class Place {
  constructor(id, ownerId, title, imageUrl, address, lat, lng) {
    this.id = id;
    this.ownerId = ownerId;
    this.title = title;
    this.imageUrl = imageUrl;
    this.address = address;
    this.lat = lat;
    this.lng = lng;
  }
}

export default Place;
